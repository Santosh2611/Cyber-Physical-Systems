% assign the volume of the cone to the variable named vol
% Cone parameters
r = 5;
h = 12;

% Compute the volume of the cone using variables r and h.
vol = (pi * r * r * h)/3;

% Compute the area of the cone using variables r and h.
area = (pi * r) * (r + sqrt(h*h + r*r));
